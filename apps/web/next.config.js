/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // API routes are now in Next.js, so we use relative paths
  // This works both locally and on Vercel
};

module.exports = nextConfig;

